# TODO: Fix this in the future. It's not very nice but most things wil probably need utility functions
include_directories(utility/include)

add_subdirectory(logger)
add_subdirectory(entrypoint)
add_subdirectory(event_system)
